numpy>=1.24,<3.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0
scikit-learn>=1.3,<2.0
lightgbm>=4.0,<5.0   # Alternative cross-sectional return predictor (src/lightgbm_model.py)
shap>=0.45,<1.0      # SHAP feature attribution for LightGBM walk-forward explainability (src/shap_attribution.py)
weasyprint>=60.0     # HTML → PDF engine for tearsheet (requires pango/gobject system libs)
fpdf2>=2.7,<3.0      # Pure-Python PDF fallback if WeasyPrint system libs unavailable
plotly>=5.18,<6.0
arch>=5.3,<7.0
optuna>=3.6.0           # Bayesian hyperparameter optimization (scripts/run_hyperopt.py)
jupyter>=1.0
ipython>=8.0
pytest>=7.4

# Optional provider-specific dependencies (install only the one(s) you use)
# Yahoo Finance provider (includes free macro via FRED + Banxico + INEGI)
yfinance>=0.2.36
pandas_datareader>=0.10  # needed for FRED macro data
pyyaml>=6.0             # needed for config.yaml
python-dotenv>=1.0      # needed for .env loading in run_all.py
# Bloomberg provider (requires Bloomberg Terminal + BLPAPI C++ SDK)
# xbbg>=0.7.8
# Refinitiv / LSEG provider
# Requires an active LSEG Workspace subscription + lseg-data.config.json
# with your App Key (obtain at developers.lseg.com).
lseg-data>=2.1.1
